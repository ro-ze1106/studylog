<% if @feed_items.any? %>
  <ol class="problems">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items, previous_label: '&#8592; &nbsp;前', next_label: '次 &#8594;', inner_window: 1, outer_window: 0 %>
<% end %>