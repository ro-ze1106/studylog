<div id="favorite-<%= @problem.id %>">
  <% if !current_user.nil? && current_user.favorite?(@problem) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>